extends layouts/main-layout.pug

block styles
    link(rel="stylesheet", href="/css/main.css")

block content
    main.main-content
        if users.length > 0
            ul.user-list
                each user in users
                    li.user-item= user.name
        else
            h1 No users